#include <iostream>
using namespace std;
int main(){
//char numeros[] = {'e', 'a', 'm', 'b', 's'};
//int numeros[] = {2, 3, 4, 5, 1};
string nombre[] = {"Jose", "Alejandro", "Zeus", "Mario", "Luigi"};
int i, j, aux, min;


	
	/*for (int i = 0; i < 5; i++){
		min  = i;
		for (int j = i+1; j < 5; j++){
			if(numeros[j] < numeros[min]){
				min = j;
			}
		}

		aux = numeros[i];
		numeros[i] = numeros[min];
		numeros[min] = aux;
	}

	cout<<"ARREGLO ORDENADO: ";
	for (int i = 0; i < 5; i++){
		cout<<numeros[i]<<" ";
	}

	cout<<endl<<endl;*/

	return 0;
}
